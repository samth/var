structs

don't eta-expand in the annotator -- breaks some things we can check in `contract-in'

evaluate Okasaki

fix soundness bug with predicates that have abstract parts -- don't remember them

test: all valid programs reduce to a value or blame.

state the main theorem and randomly test

first class contracts
state
translator from Racket subset

drop labels in abstract values (makes more things the same, speeding
up convergence)

parameterize cesk to be abstract or not

pony
